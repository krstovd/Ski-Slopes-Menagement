# Задача

Потребно е да се развие веб апликација за управување со ски патеки.

## Функционални барања

- Потребно е на патеките `/` и `/ski-slopes` да прикажете листа од сите креирани ски патеки користејќи го
  темплејтот `list.html`. Дополнително тука треба да овозможите пагинација на ски патеките.
    - Имплементацијата на оваа функционалност може да ја проверите со тестовите `test_list_10pt`
      и `test_pagination_10pt`.

- Потребно е да се имплементира додавање на нова ски патека. При клик на копчето **Add new ski slope** од
  темплејтот `list.html`, потребно е да се прикаже темплејтот `form.html` на патеката `/ski-slopes/add`, каде при клик
  на **Submit** ќе се креира и запише нова ски патека во базата на податоци. Потоа треба да се прикаже
  страната `/ski-slopes`.
    - Имплементацијата на оваа функционалност може да ја проверите со тестовите `test_create_10pt`
      и `test_create_mvc_10pt`.

- Потребно е да се имплементира бришење на ски патека. При клик на копчето **Delete** од
  темплејтот `list.html`, потребно е да се избрише ски патеката од базата на податоци. Потоа треба да се прикаже
  страната `/ski-slopes`.
    - Имплементацијата на оваа функционалност може да ја проверите со тестовите `test_delete_3pt`
      и `test_delete_mvc_2pt`.

- Потребно е да се имплементира промена на детали за ски патеката. При клик на копчето **Edit** од
  темплејтот `list.html`, потребно е да се прикаже темплејтот `form.html` на патеката `/ski-slopes/edit/[id]`, при што
  во `<input>` елементите ќе се прикажат вредностите за ски патеката што се менува. При клик на **Submit** треба да
  се запише промената на ски патеката во базата на податоци. Потоа треба да се прикаже страната `/ski-slopes`.
    - Имплементацијата на оваа функционалност може да ја проверите со тестовите `test_edit_10pt` и `test_edit_mvc_10pt`.

- Потребно е да се имплементира затворање на ски патека, доколку патеката е отворена. При клик на
  копчето **Close** од темплејтот `list.html`, потребно е да се промени соодветното поле од моделот. Потоа
  треба да се прикаже
  страната `/ski-slopes`.
    - Имплементацијата на оваа функционалност може да ја проверите со тестовите `test_close_slope_3pt`
      и `test_close_slope_mvc_2pt`.

- Потребно е да конфигурирате најава на `/login` и одјава на `/logout`. Притоа, јавни се патеките `/`
  и `/ski-slopes`, додека сите останати страници треба да се видливи само за корисник со улога admin. Дополнително,
  кај `list.html` копчињата **Edit**, **Delete**, **Add new ski slope** и **Close** треба да се видливи само за корисник
  кој има улога admin. Копчето **Close** треба да е видливо само дококу патеката е отворена.
    - Имплементацијата на оваа функционалност може да ја проверите со
      тестовите `test_security_urls_10pt` и `test_security_buttons_10pt`.

- Потребно е да овозможите пребарување на ски патеките по име (дали испратениот текст се
  содржи во променлицата `name`), должина на ски патека (ски патеки подолги од внесената должина),
  тежина на ски патека и ски центар преку формата со `id="filter-form"` во темплејтот `list.html`. Резултатите од
  пребарувањето треба да се прикажат на истата страница, при што ќе се прикажат само ски патеки чии имиња го содржат
  испратениот текст, имаат поголема должина од внесената, се од избраната тежина и припаѓаат на избраниот ски центар.
  Филтрирањето се извршува само според внесените полиња (ако се празни, се игнорира филтрирањето по тој критериум).
    - Имплементацијата на оваа функционалност може да ја проверите со тестовите `test_filter_5pt`
      и `test_filter_service_5pt`.

## Техничко упатство:

- Во пакетот `mk.ukim.finki.wp.kol2025g2.model` веќе се креирани класите кои го репрезентираат моделот.
  Потребно е да извршите нивно мапирање со соодветните JPA анотации за моделот успешно да се сними во базата на
  податоци.
  Притоа важат следните услови:
    - Една ски патека припаѓа на точно еден ски центар, додека пак еден ски центар може да има повеќе ски патеки.
    - Идентификаторите за `SkiSlope` и `SkiResort` треба да бидат автоматски генерирани.
- Во пакетот `mk.ukim.finki.wp.exam.kol2025g2.service` се веќе дефинирани интерфејсите за сервисната логика.
  За секој од методите имате опис што треба да биде имплементирано. Потребно е да се имплементираат овие интерфејси во
  соодветните класи во пакетот `mk.ukim.finki.wp.exam.kol2025g2.service.impl`. Во коментарите на методите се објаснети
  дополнителни услови (доколку ги има) кои треба да ги исполни методот.
- Класите од пакетот `mk.ukim.finki.wp.exam.kol2025g2.repository` треба да ги дополните со потребните методи кои ви се
  потребни за да ја овозможите функционалноста на имплементацијата на сервисниот слој. Тие треба да се изведат од
  класата `JpaRepository` од Spring Data.
- Потребно е да ја анотирате класата `DataInitializer` и нејзините соодветни методи, така што при стартување на
  апликацијата ќе се изврши методот `initData`.
- Во класата `mk.ukim.finki.wp.exam.kol2025g2.SkiSlopeController` се дефинирани сите методи кои се потребни.
  За секој од методите имате опис што треба да биде имплементирано. Потребно е овие handler методи да ги мапирате со
  користење само на HTTP GET и POST барања.
- Дополнете ги темплејтите со соодветните **Thymeleaf** атрибути за да се постигнат бараните функционалности.
  Притоа, ако недостасуваат одредени елементи и атрибути, може да ги додадете, но **НЕ СМЕЕ** да ги менувате `id`
  и `class` својствата на тековните елементи.
  Во коментари се дадени описи за елементите кои треба да се повторуваат, како и кои методи од контролерот треба да се
  повикаат.
- Потребно е да конфигурирате најава и одјава на корисници со Spring Security во класата `SecurityConfig`.
  Во самата класа има опис што треба да биде имплементирано.
